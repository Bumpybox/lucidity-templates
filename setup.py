from setuptools import setup

setup(name='pipeline-schema',
      version='0.1',
      description='Bumpybox pipeline schema',
      url='https://github.com/Bumpybox/pipeline-schema',
      author='Toke Jepsen',
      author_email='tokejepsen@bumpybox.com',
      license='MIT',
      packages=['pipeline_schema'],
      zip_safe=False)
